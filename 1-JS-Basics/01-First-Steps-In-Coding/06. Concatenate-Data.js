function concatenateData(input){
    let firstName = input[0];
    let lastName = input[1];
    let userAge = input[2];
    let userLocation = input[3];

    console.log(`You are ${firstName} ${lastName}, a ${userAge}-years old person from ${userLocation}.`);
}
